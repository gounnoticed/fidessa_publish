
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_price_deviates() {
        /*
        let mut lastp: f64 = 0.0;
        let mut st = String::from("2200.8");

        assert!(price_deviates(st, &mut lastp));
        st = String::from("2200.9");
        assert!(!price_deviates(st, &mut lastp));
        st = String::from("2201.9");
        assert!(!price_deviates(st, &mut lastp));
        st = String::from("2203.9");
        assert!(!price_deviates(st, &mut lastp));
        st = String::from("2303.9");
        assert!(price_deviates(st, &mut lastp));
*/
    }
}
